# Runtime Contracts

This package is reserved for implementation-facing Pydantic models that mirror
the schemas in `docs/contracts/`.

Future sessions should add models here before implementing stage logic when
validation is needed.
